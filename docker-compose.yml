version: '3.8'

services:
  telegram_bot:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - DOTA_API_KEY=${DOTA_API_KEY}
    volumes:
      - ./app:/app
    env_file:
      - .env
    ports:
      - "80:80"
    restart: always